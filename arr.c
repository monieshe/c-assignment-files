#include <stdio.h>

int main() {
    int arr[7] = {10, 20, 30, 40, 50, 60, 70};

    printf("Array elements:\n");
    for (int i = 0; i <= 7; i++) {
        printf("%d ", arr[i]);
    }
    return 0;
}
